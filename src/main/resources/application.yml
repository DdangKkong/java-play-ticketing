spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/payment?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: payment
    password: 1234qwer!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    database: mysql

    properties:
      hibernate:
        format_sql: true

  profiles:
    active: local
    include: API-KEY
logging:
  level:
    root: INFO
